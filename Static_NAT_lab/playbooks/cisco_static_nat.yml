---
- name: NAT configs
  hosts: NAT
  gather_facts: false
  tasks:
    - name: Accept NAT config for the router
      cisco.ios.ios_config:
        lines: "{{ lookup('template', 'cisco_static_nat.j2')}}"
